<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Primary-workspace-only resources. Secondary windows and dialogs keep
         using the proven global templates from Light.xaml. -->
    <Color x:Key="AppicaSelectedBorderColor">#AFC9F8</Color>
    <Color x:Key="AppicaPanelBorderColor">#DDE3EC</Color>

    <SolidColorBrush x:Key="AppicaSelectedBorderBrush"
                     Color="{StaticResource AppicaSelectedBorderColor}" />
    <SolidColorBrush x:Key="AppicaPanelBorderBrush"
                     Color="{StaticResource AppicaPanelBorderColor}" />

    <DropShadowEffect x:Key="AppicaPrimaryPanelShadowEffect"
                      BlurRadius="18"
                      Direction="270"
                      Opacity="0.07"
                      ShadowDepth="2"
                      Color="#2030445C" />
    <DropShadowEffect x:Key="AppicaPopupShadowEffect"
                      BlurRadius="16"
                      Direction="270"
                      Opacity="0.16"
                      ShadowDepth="4"
                      Color="#2B182230" />

    <Style x:Key="AppicaPanelSurfaceStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <Style x:Key="AppicaChatSidebarStyle"
           TargetType="Border"
           BasedOn="{StaticResource AppicaPanelSurfaceStyle}" />

    <Style x:Key="AppicaChatWorkspaceStyle"
           TargetType="Border"
           BasedOn="{StaticResource AppicaPanelSurfaceStyle}" />

    <Style x:Key="AppicaChatHeaderStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="100" />
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="24,14" />
    </Style>

    <Style x:Key="AppicaChatInspectorStyle"
           TargetType="Border"
           BasedOn="{StaticResource AppicaPanelSurfaceStyle}" />

    <Style x:Key="AppicaChatComposerStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="140" />
        <Setter Property="Margin" Value="14,10,14,14" />
        <Setter Property="Padding" Value="14,11" />
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaSelectedBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Effect" Value="{StaticResource AppicaPrimaryPanelShadowEffect}" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AppicaConversationSearchStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource ModernTextBoxStyle}">
        <Setter Property="Height" Value="44" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="AppicaConversationGroupButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource ModernButtonBaseStyle}">
        <Setter Property="MinHeight" Value="78" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Padding" Value="7,8" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>

    <Style x:Key="AppicaConversationItemStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="72" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Padding" Value="13,9" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <Border x:Name="SelectionRail"
                                Width="3"
                                Margin="0,8,0,8"
                                HorizontalAlignment="Left"
                                Background="{DynamicResource AccentBrush}"
                                CornerRadius="0,3,3,0"
                                Opacity="0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome"
                                    Property="Background"
                                    Value="{DynamicResource ControlHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome"
                                    Property="Background"
                                    Value="{DynamicResource ControlPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Chrome"
                                    Property="BorderBrush"
                                    Value="{DynamicResource FocusRingBrush}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="Chrome"
                                    Property="Background"
                                    Value="{DynamicResource AccentSoftBrush}" />
                            <Setter TargetName="Chrome"
                                    Property="BorderBrush"
                                    Value="{DynamicResource AppicaSelectedBorderBrush}" />
                            <Setter TargetName="SelectionRail" Property="Opacity" Value="1" />
                        </DataTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AppicaHeaderIconButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource ModernButtonBaseStyle}">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="44" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="18" />
    </Style>

    <Style x:Key="AppicaHeaderActionButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource SecondaryButtonStyle}">
        <Setter Property="Height" Value="44" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="14,7" />
    </Style>

    <Style x:Key="AppicaMessageAvatarStyle" TargetType="Border">
        <Setter Property="Width" Value="42" />
        <Setter Property="Height" Value="42" />
        <Setter Property="CornerRadius" Value="21" />
        <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ClipToBounds" Value="True" />
    </Style>

    <Style x:Key="AppicaMessageNameStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

    <Style x:Key="AppicaMessageTimeStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="9,0,0,0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
    </Style>

    <Style x:Key="AppicaMessageBubbleStyle" TargetType="Border">
        <Setter Property="Padding" Value="15,11" />
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="9" />
    </Style>

    <Style x:Key="AppicaComposerTextBoxStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource ModernTextBoxStyle}">
        <Setter Property="MinHeight" Value="62" />
        <Setter Property="MaxHeight" Value="150" />
        <Setter Property="Padding" Value="2,3" />
        <Setter Property="Background" Value="{DynamicResource ComposerHintBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaSelectedBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AppicaComposerMetaTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="AppicaTokenStatusTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource AppicaComposerMetaTextStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsEstimatedOverLimit}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AppicaPopupSurfaceStyle" TargetType="Border">
        <Setter Property="Padding" Value="7" />
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Effect" Value="{StaticResource AppicaPopupShadowEffect}" />
    </Style>

    <Style x:Key="AppicaInspectorTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="64" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border BorderBrush="{DynamicResource AppicaPanelBorderBrush}"
                                BorderThickness="0,0,0,1">
                            <ItemsPresenter Margin="6,0" />
                        </Border>
                        <ContentPresenter x:Name="PART_SelectedContentHost"
                                          Grid.Row="1"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding SelectedContent}"
                                          ContentSource="SelectedContent"
                                          ContentStringFormat="{TemplateBinding SelectedContentStringFormat}"
                                          ContentTemplate="{TemplateBinding SelectedContentTemplate}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AppicaInspectorTabItemStyle" TargetType="TabItem">
        <Setter Property="MinHeight" Value="64" />
        <Setter Property="Padding" Value="7,8" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid x:Name="Root" Background="Transparent">
                        <Border x:Name="HoverSurface"
                                Margin="2,8"
                                Background="Transparent"
                                CornerRadius="7" />
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          TextElement.FontWeight="{TemplateBinding FontWeight}" />
                        <Border x:Name="SelectionIndicator"
                                Height="2"
                                Margin="8,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Bottom"
                                Background="{DynamicResource AccentBrush}"
                                CornerRadius="1"
                                Visibility="Collapsed" />
                        <Border x:Name="FocusRing"
                                Margin="5,7"
                                BorderBrush="{DynamicResource FocusRingBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Opacity="0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HoverSurface"
                                    Property="Background"
                                    Value="{DynamicResource ControlHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter TargetName="SelectionIndicator" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="FocusRing" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AppicaInspectorCardStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,0,14" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
    </Style>

    <Style x:Key="AppicaInspectorInsetStyle" TargetType="Border">
        <Setter Property="Padding" Value="11" />
        <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppicaPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
    </Style>

    <Style x:Key="AppicaInspectorCardTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

    <Style x:Key="AppicaInspectorBodyScrollStyle" TargetType="ScrollViewer">
        <Setter Property="Margin" Value="18" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style x:Key="AppicaTokenProgressStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="8" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
        <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4" />
                        <Border x:Name="PART_Indicator"
                                HorizontalAlignment="Left"
                                Background="{TemplateBinding Foreground}"
                                CornerRadius="4" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding EstimatedTokenUsageLevel}" Value="Warning">
                <Setter Property="Foreground" Value="{DynamicResource WarningBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding EstimatedTokenUsageLevel}" Value="Danger">
                <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AppicaDashboardCardStyle"
           TargetType="Border"
           BasedOn="{StaticResource CardStyle}">
        <Setter Property="Background" Value="{DynamicResource SurfaceSolidBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="Effect" Value="{StaticResource AppicaPrimaryPanelShadowEffect}" />
    </Style>

    <Style x:Key="AppicaDashboardRecentButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource ModernButtonBaseStyle}">
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>

    <Style x:Key="AppicaPaneTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

    <Style x:Key="AppicaEyebrowTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
    </Style>
</ResourceDictionary>
