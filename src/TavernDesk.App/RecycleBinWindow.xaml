<Window x:Class="TavernDesk.App.RecycleBinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="消息回收箱"
        Width="920"
        Height="620"
        MinWidth="680"
        MinHeight="440"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="消息回收箱" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="此处只提供永久删除，不恢复消息。"
                       Foreground="{StaticResource MutedTextBrush}" />
        </StackPanel>

        <TextBox x:Name="FilterBox"
                 Grid.Row="1"
                 Margin="0,18,0,12"
                 TextChanged="FilterBox_OnTextChanged"
                 ToolTip="按会话、发送者或正文过滤" />

        <DataGrid x:Name="DeletedMessagesGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="会话" Binding="{Binding ConversationTitle}" Width="180" />
                <DataGridTextColumn Header="发送者" Binding="{Binding SenderKind}" Width="90" />
                <DataGridTextColumn Header="正文" Binding="{Binding Content}" Width="*" />
                <DataGridTextColumn Header="删除时间"
                                    Binding="{Binding DeletedAt, StringFormat=yyyy-MM-dd HH:mm}"
                                    Width="145" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="永久删除选中"
                        Click="DeleteSelected_OnClick"
                        Style="{StaticResource SecondaryButtonStyle}" />
                <Button Margin="8,0,0,0"
                        Content="清空回收箱"
                        Click="ClearAll_OnClick"
                        Foreground="{StaticResource DangerBrush}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
            <Button Grid.Column="1"
                    Width="100"
                    Content="关闭"
                    IsCancel="True"
                    Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>
    </Grid>
</Window>
