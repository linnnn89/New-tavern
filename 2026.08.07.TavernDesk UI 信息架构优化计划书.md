# TavernDesk UI 信息架构优化计划书
## 版本代号：R2-C UI Cleanup

> **当前状态（2026-08-07）**：本计划的工作包 1–8 已按当前源码执行完成；工作包 9“全局默认行为持久化”暂不实施。根目录 README 已同步最终页面结构、跑团上下文链路和剩余边界。代码侧已完成 Release 构建、确定性 `app/` 自包含发布与 `git diff --check`；本轮不由代理执行截图，布局由用户按 README 清单人工查验。

### 一、任务目标

本轮不是视觉重做，也不是增加新功能。

本轮只解决三个问题：

1. **高频操作更快**
   - 每次聊天、每轮跑团经常使用的功能保持直接可见。
   - 不因为“页面整洁”反而增加用户点击次数。

2. **低频设置不长期占据主工作区**
   - 一次配置后长期不修改的参数进入“设置”。
   - 当前会话/当前跑团偶尔需要调整的参数，可在模块高级设置中保留入口。
   - 不建立重复的第三套配置。

3. **建立统一的信息层级**
   - 主界面负责“做事情”。
   - 模块高级区负责“调整当前对象”。
   - 设置负责“全局默认和低频专业配置”。

---

# 二、禁止事项

执行人员必须遵守：

- 不改变聊天、跑团、记忆、世界书的底层业务语义。
- 不修改 Provider 请求逻辑。
- 不修改数据库 schema，除非进入后续明确批准的“默认行为持久化”任务。
- 不引入新 UI 框架。
- 不新增第三方依赖。
- 不增加 Agent、多 Agent、消息总线、通用导航框架。
- 不为了减少 XAML 行数进行无关组件化。
- 不把已有功能删除。
- 不新增重复入口。
- 不因为“更现代”随意加入更多按钮、动画、弹窗。
- 不修改与当前工作包无关的页面。

尤其注意：

**聊天页面已经存在右下角侧边功能面板折叠按钮。禁止再增加“聊天设置”“检查器”“展开侧栏”等重复按钮。**

---

# 三、统一的功能分级规则

以后所有 UI 功能先按使用频率分类，再决定放在哪里。

## A级：每次使用都可能操作

要求：**0–1 次点击可达，直接放主界面。**

例如：

- 发送消息；
- 提交跑团行动；
- 选择需要行动的 AI 玩家；
- GM 裁定；
- 开始聊天；
- 开始跑团；
- 返回；
- 当前必要的模型/模式切换，如果本模块本身支持即时切换。

原则：

> 不能为了页面“干净”把真正高频的操作藏起来。

---

## B级：当前对象经常调整

要求：**最多 1–2 次点击。**

放置：

- 已有右侧面板；
- Expander；
- 当前对象设置弹窗；
- 小型菜单。

例如：

- 当前聊天 Persona；
- 当前聊天召回 ON/OFF；
- 当前跑团记忆 ON/OFF；
- 当前跑团 Context Budget；
- 当前 AI 席位模型；
- GM 模型；
- 当前群聊接力方式。

这类功能可以同时存在：

```text
设置 → 全局默认
+
模块 → 当前对象覆盖
```

但必须使用：

```text
Default → Override
```

关系，不能形成两份互不相关的配置。

---

## C级：设置一次后很少修改

只放：

```text
设置
```

例如：

- API Provider；
- API Key；
- 模型目录；
- 功能模型分配；
- 全局 Prompt；
- 记忆默认策略；
- 召回默认参数；
- 数据目录；
- 全局字体。

模块页面最多提供：

```text
打开相关设置 →
```

禁止复制完整参数编辑器。

---

## D级：专业诊断

只有真正用于当前请求判断时才在模块显示。

例如：

```text
当前 Token
本轮 Context 分段
当前召回结果
当前生成状态
```

其他长期调试参数不应该占主 UI。

---

# 四、统一的“双入口”规则

只有以下情况允许设置页与模块同时出现。

## 全局默认 + 当前对象覆盖

例如：

```text
设置
默认跑团预算：15000

某局跑团
当前局预算：20000
```

或者：

```text
设置
默认召回：ON

当前聊天
召回：OFF
```

模块 UI 必须能够明确表达：

```text
使用默认值
```

或：

```text
当前已覆盖
```

以后若尚未实现这种继承机制，本轮不要自行发明数据库设计。

---

# 五、一级导航

保持当前结构：

```text
仪表盘
聊天
跑团
角色
世界书
设置
```

不要新增：

```text
AI
模型
记忆
Prompt
```

作为一级入口。

这些是支撑能力，不是用户的主要任务。

---

# 六、工作包 1：聊天页右侧面板整理（已完成）

## 目标

不改变现有：

```text
会话列表 | 聊天正文 | 可折叠右侧面板
```

也不增加任何新的主界面按钮。

现有侧边栏折叠按钮继续作为唯一展开入口。

---

## 当前问题

右侧目前同时包含：

```text
上下文
召回
角色 Prompt
Persona
记忆
群聊
```

其中同时混入：

- 状态查看；
- 高频控制；
- 低频高级参数；
- 全局 Prompt 跳转。

层级不统一。

---

## 调整目标

右侧一级 Tab 调整为：

```text
上下文
角色
记忆
会话
```

### 1. 上下文

保留：

```text
本轮 Token 估算
Context 分段
API 请求结构
本轮召回结果
```

不要在这里长期放：

```text
召回最大条数
近期原文消息数
召回 Token Budget
```

这些属于低频参数。

本页只允许有：

```text
召回：ON/OFF
```

如果这个开关本身确实经常使用。

高级参数提供：

```text
高级召回设置 →
```

跳转到设置对应位置。

---

### 2. 角色

合并现有：

```text
角色 Prompt
Persona
```

建议结构：

```text
当前角色

角色 System Prompt
只显示摘要/只读预览
[编辑角色]

历史后指令
只显示摘要
[编辑]

----------------

USER Persona

名称
简介
[保存]
```

Persona 属于较常调整内容，可以保留编辑。

角色 System/Post-history 不需要一直用两个大型编辑框占空间。

---

### 3. 记忆

默认显示：

```text
长期记忆

状态
当前正文大小
checkpoint
下一次自动更新时间/进度

[查看/编辑正文]
[立即生成更新]
```

不要默认展示：

```text
更新间隔
单批轮数
仅新增
目标 Token
更新 Prompt
压缩 Prompt
请求 payload
```

这些移入设置。

草稿存在时，再动态显示：

```text
待确认草稿
```

不是永久占据一级页面。

---

### 4. 会话

普通聊天：

```text
当前会话基本状态
发送模式
显示模式
```

群聊时追加：

```text
接力状态
当前发言者
暂停/继续
下一发言者
```

群聊高级参数：

```text
自动接力上限
@USER 暂停规则
成员管理
```

可保留在这一页的折叠“高级”区域，或者跳转设置。

不要再独立增加第五个“群聊”一级 Tab。

---

## 验收

只检查：

1. 现有右侧折叠按钮仍正常；
2. 不新增第二个侧栏入口；
3. 单聊下只显示合理的 4 个分类；
4. 群聊相关信息只在群聊时出现；
5. 主聊天区域不变窄；
6. 原有功能没有消失；
7. 1440×900 和约 1100×700 下没有明显横向溢出。

### 测试要求

如果只是 XAML 重排：

```text
编译通过
+
人工截图确认
```

即可。

**不要为单纯布局修改大量编写 ViewModel 单元测试。**

---

# 七、工作包 2：聊天顶部减负（已完成）

## 目标

高频功能留下，低频文件操作移走。

当前类似：

```text
导入 JSONL
导出当前
显示模式
```

其中：

- 显示模式：可能经常使用；
- JSONL 导入导出：明显低频。

建议改成：

```text
显示模式 ▼      ⋯
```

`⋯`：

```text
导入聊天
导出当前聊天
```

已有右键能做的操作不要再塞进去形成重复入口。

---

## 验收

- 导入/导出都仍然最多 2 次点击；
- 显示模式仍然 1 次点击；
- 不修改 JSONL 实际逻辑。

---

# 八、工作包 3：跑团游玩页（已完成）

## 目标

保留当前三栏：

```text
玩家席位 | 跑团记录 | 当前步骤
```

这是合理布局，不重做。

---

## 顶部

当前类似：

```text
记忆设置
重新载入本局
返回剧本库
```

调整为：

```text
跑团设置
返回剧本库
⋯
```

`⋯`：

```text
重新载入本局
```

以后真的存在导出/诊断功能，再加入菜单。

禁止现在创建空菜单项。

---

## “记忆设置”改名

改为：

```text
跑团设置
```

因为当前弹窗已经包含：

- Context 总预算；
- 玩家历史预算；
- GM 历史预算；
- Memory ON/OFF；
- Memory 更新间隔；
- Memory Token 阈值。

已经明显不只是“记忆”。

---

## 当前步骤栏

保留已有：

```text
本轮上下文估算
额外掷骰
GM 模型设置
```

作为 Expander。

这些属于当前跑团中偶尔使用的 B 类功能。

不要弹窗化。

---

## 验收

- 每轮主要操作的点击次数不增加；
- 提交行动/生成 AI/GM 裁定位置不移动到二级菜单；
- 重新载入降低视觉优先级；
- 跑团设置仍最多 1 次点击。

---

# 九、工作包 4：跑团开局大厅减负（已完成）

## 主界面必须直接显示

这些每局创建基本都会使用：

```text
本局标题
流程方式
谁担任 GM
GM 模型
我的参与方式
USER 名称
AI 玩家选择
每个 AI 使用的模型
是否导入该角色记忆
开始游戏
保存草稿
```

不要隐藏。

---

## 移入“高级设置”

这些不是每次都调：

```text
玩家历史 Token Budget
GM 历史 Token Budget
全局 GM Prompt 跳转
全局 AI 玩家 Prompt 跳转
```

使用一个：

```text
高级设置
```

不要增加多个小按钮。

---

## 剧本内容

大厅目前同时能看到：

```text
世界观
公开规则
开场设置
```

不应该每次开局都出现三个大编辑框。

默认改为只读摘要：

```text
世界观：……
公开规则：……
开场设置：……
```

提供一个：

```text
本局覆盖剧本内容…
```

只有用户确实想修改这局的冻结快照时才打开编辑。

---

## 验收

开局最常见流程应该是：

```text
选择剧本
→ 选择 GM
→ 选择玩家
→ 选择模型
→ 开始
```

不应该要求展开任何高级设置才能完成普通开局。

---

# 十、工作包 5：剧本编辑页面重构（已完成）

## 目标

这是创作工作区。

不要弹窗化。

不要继续使用从 1 到 9 的超长滚动页面。

改成左侧分类或 Tab。

推荐：

```text
基础
世界与规则
主持与开场
资料
```

### 基础

```text
标题
简介
```

### 世界与规则

```text
世界观
公开规则
```

### 主持与开场

```text
GM 指令
开场设置
开场旁白
```

### 资料

```text
世界书绑定
旧示例/历史归档
```

“旧示例”放折叠高级区。

---

## 验收

- 保存逻辑完全不变；
- 切换分类不丢失未保存内容；
- 用户无需滚动十几个屏幕找某个字段；
- 不新增新的数据模型。

---

# 十一、工作包 6：角色页（已完成）

## 角色书架

整体保留。

唯一建议：

当前卡片可能同时拥有：

```text
Hover：聊天
Hover：编辑
底部：进入
```

有一定重复。

调整为：

```text
点击卡片 → 进入角色主页

Hover：
聊天
编辑
```

如果修改点击行为风险较大，可以暂时不做，本项优先级较低。

---

## 角色主页

保留：

```text
继续聊天
新建聊天
编辑
替换图片
删除
```

“归类”属于中低频。

将展开的大块“书架归类”改成：

```text
归类…
```

弹小型对话框或 Popover：

```text
选择书架
加入
移除
```

---

## 角色编辑器

不要继续纵向堆所有字段。

改为：

```text
基础
对话
提示词
元数据
```

### 基础

```text
名称
描述
性格
场景
```

### 对话

```text
首次发言
备选开场
mes_example
```

### 提示词

```text
System Prompt
Post-history
Depth Prompt
```

### 元数据

```text
标签
作者说明
兼容字段
```

---

## 验收

- 所有角色卡字段仍可编辑；
- 不改变导入导出格式；
- 切换 Tab 不丢草稿；
- 保存仍只有原有保存行为。

---

# 十二、工作包 7：世界书页面（已完成）

## 当前主任务

世界书页面真正的高频任务应该只有：

```text
选择世界书
选择词条
查看词条
编辑允许编辑的内容
```

---

## 导入挂载

不要让：

```text
新导入挂载到全局/角色/跑团
```

长期占主页面。

点击：

```text
导入世界书 / 角色卡
```

后，在导入流程里询问：

```text
导入到：
全局
指定角色
指定跑团剧本
```

---

## 挂载管理

当前固定占据大块空间的：

```text
当前挂载
角色绑定
跑团剧本绑定
```

调整为摘要：

```text
挂载：3 个角色 · 2 个跑团剧本
[管理挂载…]
```

弹窗：

```text
角色
跑团剧本
```

复用现有 Binding Collections 和 Save Commands。

---

## 验收

- 世界书正文工作区明显增大；
- 挂载仍最多 1 次点击打开；
- 导入仍能直接选择作用范围；
- 不修改实际世界书 Scope/Binding 语义。

---

# 十三、工作包 8：设置页面重新分类（已完成）

当前已有功能基本足够，重点是重新分类，而不是继续堆设置。

推荐：

```text
AI 与模型
默认行为
AI 行为模板
界面
数据
```

暂时不要为了好看创建空的“高级”页。

---

## AI 与模型

内部二级：

```text
接入商
模型目录
功能分配
```

现有三个功能保持。

只是视觉归为同一大类。

---

## AI 行为模板

就是现在的：

```text
提示词管理
```

建议用户可见名称改为：

```text
AI 行为模板
```

分类仍保持：

```text
聊天
记忆
群聊
跑团
```

当前分类本身已经合理，不需要再增加层级。

同时把“分类 + 功能”两个列表合并为一个树/单列表：

```text
聊天
  角色聊天

记忆
  更新
  压缩

群聊
  接力
  记忆合并

跑团
  GM
  AI 玩家
```

右侧完整留给编辑器。

---

## 界面

保留：

```text
自动滚动
字体
字号
主题
```

可增加：

```text
聊天右侧面板：
○ 记住上次展开状态
○ 默认收起
```

不要增加：

- 阴影强度；
- 圆角；
- 动画速度；
- 十几个配色细节。

---

## 数据

保留：

```text
数据目录
```

不要因为本计划书提到“备份”就实现一个当前不存在的备份系统。

---

# 十四、工作包 9：默认行为（待决定）

这是唯一一个可以后续独立实施的“小功能型 UI 任务”。

不要和前面纯布局修改混在一起。

首先只设计：

```text
聊天默认
跑团默认
```

例如：

### 新聊天默认

```text
默认显示模式
默认自动召回 ON/OFF
```

### 新跑团默认

```text
Context Budget
Memory ON/OFF
Memory 更新间隔
Memory Token Threshold
```

注意：

如果当前数据库/设置存储不支持“全局默认 → 新对象复制”，执行人员必须停止并报告。

禁止为了完成 UI 自己设计新的通用 Settings Framework。

此工作包应在前面 UI Cleanup 完成后另行决定是否实施。

---

# 十五、执行状态与后续边界

## 第一阶段：纯 UI（已完成）

```text
1. 聊天侧栏重分类       已完成
2. 聊天顶部减负         已完成
3. 跑团游玩页顶部调整   已完成
4. 世界书挂载弹窗化     已完成
```

## 第二阶段：页面信息架构（已完成）

```text
5. 跑团大厅减负         已完成
6. 剧本编辑分页         已完成
7. 角色编辑分页         已完成
8. 设置页重新分组       已完成
```

实现保持在 XAML 和少量 UI 状态/导航同步范围内，没有修改 Service、Repository、Provider 协议或数据库 schema。

## 第三阶段：可选功能（暂缓）

```text
9. 全局默认行为         待决定
```

当前设置仓储没有“全局默认 → 新聊天/新跑团对象复制”的完整能力，因此不新增通用 Settings Framework，也不把该功能伪装成已完成。现有可持久化的聊天自动滚动设置已在设置页的“默认行为”分类中提供入口；新增跑团 Context Budget、Memory ON/OFF 等全局默认值需另行批准和设计。

---

# 十六、低价模型每个任务的标准指令

每次只给一个工作包。

统一提示：

> 只执行当前工作包。  
> 优先移动和重新组织现有 XAML，不改变业务行为。  
> 不顺手重构 ViewModel、Service、Repository。  
> 不增加新依赖。  
> 不增加重复按钮。  
> 高频操作不能因为此次优化增加点击次数。  
> 如果必须修改业务层或数据库才能完成，停止该部分并说明原因，不要自行扩大设计。  
> 修改后检查 1440×900 和较窄窗口布局。  
> 不要修改无关代码。

---

# 十七、验收策略

本轮特别强调：

## 不过度测试

### 纯 XAML 工作包

只需要：

```text
dotnet build TavernDesk.sln -c Release --no-restore
```

加人工截图检查。

不需要因为移动几个控件跑几十个业务测试。

---

### 修改 ViewModel 状态的任务

只运行对应定向测试，例如：

```text
CampaignUiStateTests
相关 Character UI tests
相关 Chat UI tests
```

不需要每个小工作包都运行完整测试全集。

---

### 最终全部完成

才执行一次：

```text
dotnet build TavernDesk.sln -c Release --no-restore
dotnet test TavernDesk.sln -c Release --no-build --no-restore
git diff --check
```

如果仓库本身存在已知既存失败：

- 记录原有失败；
- 只要求不得新增失败；
- 禁止为了“全绿”修改无关测试。

---

# 十八、截图验收

UI 项目不能只看编译。

本轮按用户要求不由代理截取或处理截图。代理只负责代码构建；用户按根目录 README 的人工查验清单自行打开聊天、跑团、角色、世界书和设置页面确认布局。以下尺寸与检查点保留为人工验收参考，不作为本轮自动化门槛。

每个涉及布局的工作包完成后，至少检查：

```text
1440×900
约 1100×700
```

重点只有：

1. 是否有横向溢出；
2. 按钮是否被挤没；
3. 主操作是否清楚；
4. 是否出现巨大空白；
5. TextBox 是否缩成不可用高度；
6. Tab/滚动区域是否正常；
7. 弹窗是否需要不必要滚动；
8. 点击路径是否比以前更长。

不要进行像素级逐点验收。

---

# 十九、最终产品原则

后续任何 UI 功能都按这句话判断：

> 用户现在是在“做事情”，还是在“配置事情”？

如果在做事情：

```text
放主界面。
```

如果是在调整当前对象，而且可能偶尔需要：

```text
放模块侧栏 / Expander / 当前对象设置。
```

如果是设置一次以后长期不变：

```text
只放设置。
```

如果是全局默认，但某个当前对象可能需要例外：

```text
设置保存默认
+
模块允许当前对象覆盖
```

如果只是专业诊断：

```text
只在真正有诊断价值的地方显示。
```

---

# 二十、本轮完成定义

本轮 UI Cleanup 完成，不要求“UI 全部重新设计”。

达到以下标准即可：

- 一级导航不增加复杂度；
- 聊天不增加新的侧栏按钮；
- 高频操作点击次数不增加；
- 聊天右栏分类从“功能堆叠”变为有逻辑的当前会话工作区；
- 跑团游玩页面保持三栏核心结构；
- 跑团大厅高级参数不再与每局必选项平铺；
- 剧本和角色长表单完成分页；
- 世界书主页面不再长期展示大块挂载管理区；
- 设置重新形成“AI与模型 / 默认行为 / 玩家人设 / AI行为模板 / 界面 / 数据”的逻辑；
- 没有引入新的业务框架；
- 没有新增依赖；
- 没有新增业务回归；
- Release 构建通过，`git diff --check` 通过；
- 主要窗口布局由用户人工查验，代理本轮未执行截图验证。

完成这些以后，再讨论颜色、动画、Mica/Acrylic、图标等纯视觉美化。

**顺序不要反过来。先解决信息架构，再做皮肤。**

## 本轮交付记录

- 已完成工作包 1–8 的 UI 信息架构调整，业务绑定和命令保持原有语义。
- AI 行为模板导航已落实为一个分组单选列表；分类标题不可选，只有当前模板条目保持蓝色选中态，不保留其他分类的灰色或蓝色历史选中。
- 模板选中条目在右侧为滚动条预留间隙，并使用圆角蓝色边框，避免窄窗口下高亮边框与滑轨重合。
- 已更新根目录 README，统一记录页面结构、跑团 Planner/Runner 链路、工程目录、构建方式和边界。
- 聊天右栏已将玩家人设独立为可发现的顶层页签；设置页新增玩家人设档案管理，多个档案共用本机个人资料存储，聊天与设置共享保存/取消编辑缓冲。
- 新增人设使用空白临时草稿，必须填写非空名称并点击保存才进入个人资料；取消或切换会丢弃未保存草稿。
- 保存遇到空白、过长、控制字符或程序保留名称时会弹出安全命名提示，建议改用普通名称；拦截内容不会写入个人资料。旧资料被自动修正时也会说明已改为“用户”并完成清理保存。
- 已增加全局嵌套滚动区滚轮路由：内层滚动区到达边界后，鼠标滚轮会继续交给父级滚动区，覆盖角色、世界书、设置、聊天和跑团页面。
- 已完成 `dotnet build TavernDesk.sln -c Release --no-restore`：0 警告、0 错误。
- 已用当前源码覆盖根目录 `app/` 确定性 `win-x64` 自包含发布目录；`app/TavernDesk.App.dll` 与发布输出哈希一致。
- 未运行自动化测试，未进行截图验证；这是本轮用户明确指定的验证范围。
- 工作包 9 保持待决定，不新增全局默认设置框架。
